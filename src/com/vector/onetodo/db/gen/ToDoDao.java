package com.vector.onetodo.db.gen;

import java.util.ArrayList;
import java.util.List;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;
import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.internal.SqlUtils;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table TO_DO.
*/
public class ToDoDao extends AbstractDao<ToDo, Long> {

    public static final String TABLENAME = "TO_DO";

    /**
     * Properties of entity ToDo.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Title = new Property(1, String.class, "title", false, "TITLE");
        public final static Property Start_date = new Property(2, Long.class, "start_date", false, "START_DATE");
        public final static Property Start_time = new Property(3, Long.class, "start_time", false, "START_TIME");
        public final static Property End_date = new Property(4, Long.class, "end_date", false, "END_DATE");
        public final static Property End_time = new Property(5, Long.class, "end_time", false, "END_TIME");
        public final static Property Repeat = new Property(6, Long.class, "repeat", false, "REPEAT");
        public final static Property Notes = new Property(7, String.class, "notes", false, "NOTES");
        public final static Property Is_allday = new Property(8, Boolean.class, "is_allday", false, "IS_ALLDAY");
        public final static Property Is_delayed = new Property(9, Boolean.class, "is_delayed", false, "IS_DELAYED");
        public final static Property Is_priority = new Property(10, Boolean.class, "is_priority", false, "IS_PRIORITY");
        public final static Property Is_align = new Property(11, Boolean.class, "is_align", false, "IS_ALIGN");
        public final static Property Is_shared = new Property(12, Boolean.class, "is_shared", false, "IS_SHARED");
        public final static Property Is_done = new Property(13, Boolean.class, "is_done", false, "IS_DONE");
        public final static Property Location = new Property(14, String.class, "location", false, "LOCATION");
        public final static Property Label_id = new Property(15, long.class, "label_id", false, "LABEL_ID");
        public final static Property Reminder_id = new Property(16, long.class, "reminder_id", false, "REMINDER_ID");
    };

    private DaoSession daoSession;


    public ToDoDao(DaoConfig config) {
        super(config);
    }
    
    public ToDoDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'TO_DO' (" + //
                "'_id' INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "'TITLE' TEXT," + // 1: title
                "'START_DATE' INTEGER," + // 2: start_date
                "'START_TIME' INTEGER," + // 3: start_time
                "'END_DATE' INTEGER," + // 4: end_date
                "'END_TIME' INTEGER," + // 5: end_time
                "'REPEAT' INTEGER," + // 6: repeat
                "'NOTES' TEXT," + // 7: notes
                "'IS_ALLDAY' INTEGER," + // 8: is_allday
                "'IS_DELAYED' INTEGER," + // 9: is_delayed
                "'IS_PRIORITY' INTEGER," + // 10: is_priority
                "'IS_ALIGN' INTEGER," + // 11: is_align
                "'IS_SHARED' INTEGER," + // 12: is_shared
                "'IS_DONE' INTEGER," + // 13: is_done
                "'LOCATION' TEXT," + // 14: location
                "'LABEL_ID' INTEGER NOT NULL ," + // 15: label_id
                "'REMINDER_ID' INTEGER NOT NULL );"); // 16: reminder_id
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'TO_DO'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, ToDo entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String title = entity.getTitle();
        if (title != null) {
            stmt.bindString(2, title);
        }
 
        Long start_date = entity.getStart_date();
        if (start_date != null) {
            stmt.bindLong(3, start_date);
        }
 
        Long start_time = entity.getStart_time();
        if (start_time != null) {
            stmt.bindLong(4, start_time);
        }
 
        Long end_date = entity.getEnd_date();
        if (end_date != null) {
            stmt.bindLong(5, end_date);
        }
 
        Long end_time = entity.getEnd_time();
        if (end_time != null) {
            stmt.bindLong(6, end_time);
        }
 
        Long repeat = entity.getRepeat();
        if (repeat != null) {
            stmt.bindLong(7, repeat);
        }
 
        String notes = entity.getNotes();
        if (notes != null) {
            stmt.bindString(8, notes);
        }
 
        Boolean is_allday = entity.getIs_allday();
        if (is_allday != null) {
            stmt.bindLong(9, is_allday ? 1l: 0l);
        }
 
        Boolean is_delayed = entity.getIs_delayed();
        if (is_delayed != null) {
            stmt.bindLong(10, is_delayed ? 1l: 0l);
        }
 
        Boolean is_priority = entity.getIs_priority();
        if (is_priority != null) {
            stmt.bindLong(11, is_priority ? 1l: 0l);
        }
 
        Boolean is_align = entity.getIs_align();
        if (is_align != null) {
            stmt.bindLong(12, is_align ? 1l: 0l);
        }
 
        Boolean is_shared = entity.getIs_shared();
        if (is_shared != null) {
            stmt.bindLong(13, is_shared ? 1l: 0l);
        }
 
        Boolean is_done = entity.getIs_done();
        if (is_done != null) {
            stmt.bindLong(14, is_done ? 1l: 0l);
        }
 
        String location = entity.getLocation();
        if (location != null) {
            stmt.bindString(15, location);
        }
        stmt.bindLong(16, entity.getLabel_id());
        stmt.bindLong(17, entity.getReminder_id());
    }

    @Override
    protected void attachEntity(ToDo entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public ToDo readEntity(Cursor cursor, int offset) {
        ToDo entity = new ToDo( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // title
            cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2), // start_date
            cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3), // start_time
            cursor.isNull(offset + 4) ? null : cursor.getLong(offset + 4), // end_date
            cursor.isNull(offset + 5) ? null : cursor.getLong(offset + 5), // end_time
            cursor.isNull(offset + 6) ? null : cursor.getLong(offset + 6), // repeat
            cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7), // notes
            cursor.isNull(offset + 8) ? null : cursor.getShort(offset + 8) != 0, // is_allday
            cursor.isNull(offset + 9) ? null : cursor.getShort(offset + 9) != 0, // is_delayed
            cursor.isNull(offset + 10) ? null : cursor.getShort(offset + 10) != 0, // is_priority
            cursor.isNull(offset + 11) ? null : cursor.getShort(offset + 11) != 0, // is_align
            cursor.isNull(offset + 12) ? null : cursor.getShort(offset + 12) != 0, // is_shared
            cursor.isNull(offset + 13) ? null : cursor.getShort(offset + 13) != 0, // is_done
            cursor.isNull(offset + 14) ? null : cursor.getString(offset + 14), // location
            cursor.getLong(offset + 15), // label_id
            cursor.getLong(offset + 16) // reminder_id
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, ToDo entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setTitle(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setStart_date(cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2));
        entity.setStart_time(cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3));
        entity.setEnd_date(cursor.isNull(offset + 4) ? null : cursor.getLong(offset + 4));
        entity.setEnd_time(cursor.isNull(offset + 5) ? null : cursor.getLong(offset + 5));
        entity.setRepeat(cursor.isNull(offset + 6) ? null : cursor.getLong(offset + 6));
        entity.setNotes(cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7));
        entity.setIs_allday(cursor.isNull(offset + 8) ? null : cursor.getShort(offset + 8) != 0);
        entity.setIs_delayed(cursor.isNull(offset + 9) ? null : cursor.getShort(offset + 9) != 0);
        entity.setIs_priority(cursor.isNull(offset + 10) ? null : cursor.getShort(offset + 10) != 0);
        entity.setIs_align(cursor.isNull(offset + 11) ? null : cursor.getShort(offset + 11) != 0);
        entity.setIs_shared(cursor.isNull(offset + 12) ? null : cursor.getShort(offset + 12) != 0);
        entity.setIs_done(cursor.isNull(offset + 13) ? null : cursor.getShort(offset + 13) != 0);
        entity.setLocation(cursor.isNull(offset + 14) ? null : cursor.getString(offset + 14));
        entity.setLabel_id(cursor.getLong(offset + 15));
        entity.setReminder_id(cursor.getLong(offset + 16));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(ToDo entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(ToDo entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getLabelDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getReminderDao().getAllColumns());
            builder.append(" FROM TO_DO T");
            builder.append(" LEFT JOIN LABEL T0 ON T.'LABEL_ID'=T0.'_id'");
            builder.append(" LEFT JOIN REMINDER T1 ON T.'REMINDER_ID'=T1.'_id'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected ToDo loadCurrentDeep(Cursor cursor, boolean lock) {
        ToDo entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Label label = loadCurrentOther(daoSession.getLabelDao(), cursor, offset);
         if(label != null) {
            entity.setLabel(label);
        }
        offset += daoSession.getLabelDao().getAllColumns().length;

        Reminder reminder = loadCurrentOther(daoSession.getReminderDao(), cursor, offset);
         if(reminder != null) {
            entity.setReminder(reminder);
        }

        return entity;    
    }

    public ToDo loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<ToDo> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<ToDo> list = new ArrayList<ToDo>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<ToDo> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<ToDo> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
