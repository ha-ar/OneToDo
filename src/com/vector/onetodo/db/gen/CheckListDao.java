package com.vector.onetodo.db.gen;

import java.util.List;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;
import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table CHECK_LIST.
*/
public class CheckListDao extends AbstractDao<CheckList, Long> {

    public static final String TABLENAME = "CHECK_LIST";

    /**
     * Properties of entity CheckList.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Title = new Property(1, String.class, "title", false, "TITLE");
        public final static Property Ischecked = new Property(2, Boolean.class, "ischecked", false, "ISCHECKED");
        public final static Property Todo_id = new Property(3, long.class, "todo_id", false, "TODO_ID");
    };

    private Query<CheckList> toDo_CheckListListQuery;

    public CheckListDao(DaoConfig config) {
        super(config);
    }
    
    public CheckListDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'CHECK_LIST' (" + //
                "'_id' INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "'TITLE' TEXT," + // 1: title
                "'ISCHECKED' INTEGER," + // 2: ischecked
                "'TODO_ID' INTEGER NOT NULL );"); // 3: todo_id
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'CHECK_LIST'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, CheckList entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String title = entity.getTitle();
        if (title != null) {
            stmt.bindString(2, title);
        }
 
        Boolean ischecked = entity.getIschecked();
        if (ischecked != null) {
            stmt.bindLong(3, ischecked ? 1l: 0l);
        }
        stmt.bindLong(4, entity.getTodo_id());
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public CheckList readEntity(Cursor cursor, int offset) {
        CheckList entity = new CheckList( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // title
            cursor.isNull(offset + 2) ? null : cursor.getShort(offset + 2) != 0, // ischecked
            cursor.getLong(offset + 3) // todo_id
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, CheckList entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setTitle(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setIschecked(cursor.isNull(offset + 2) ? null : cursor.getShort(offset + 2) != 0);
        entity.setTodo_id(cursor.getLong(offset + 3));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(CheckList entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(CheckList entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "checkListList" to-many relationship of ToDo. */
    public List<CheckList> _queryToDo_CheckListList(long todo_id) {
        synchronized (this) {
            if (toDo_CheckListListQuery == null) {
                QueryBuilder<CheckList> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.Todo_id.eq(null));
                toDo_CheckListListQuery = queryBuilder.build();
            }
        }
        Query<CheckList> query = toDo_CheckListListQuery.forCurrentThread();
        query.setParameter(0, todo_id);
        return query.list();
    }

}
